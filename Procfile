# Procfile for Heroku (alternative to heroku.yml if not using Docker)
# Uncomment the line below if deploying without Docker:
# web: cd Backend && uvicorn src.main:app --host 0.0.0.0 --port $PORT

# Note: When using heroku.yml (Docker-based deployment), this file is optional
# Docker deployment is recommended for consistent environments
