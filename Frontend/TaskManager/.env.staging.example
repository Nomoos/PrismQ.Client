# Staging Environment Configuration Template
# Frontend/TaskManager - Staging Environment
#
# Copy this file to .env when building for staging:
#   cp .env.staging.example .env
#   ./build-and-package.sh

# =============================================================================
# API Configuration
# =============================================================================

# Staging Backend API URL
# Point to your staging backend deployment
VITE_API_BASE_URL=https://api-staging.your-domain.com

# Alternative: If backend is in subdirectory
# VITE_API_BASE_URL=https://your-domain.com/api-staging

# API Key (if required)
# Use staging-specific API key
VITE_API_KEY=staging-test-key-replace-me

# =============================================================================
# Application Configuration
# =============================================================================

# Environment identifier
VITE_APP_ENV=staging

# Application name (with environment indicator)
VITE_APP_NAME=PrismQ TaskManager (Staging)

# Base URL (if deployed to subdirectory)
# Leave empty for root deployment
VITE_BASE_URL=/

# For subdirectory: /staging/
# VITE_BASE_URL=/staging/

# =============================================================================
# Feature Flags
# =============================================================================

# Enable analytics (usually disabled in staging)
VITE_ENABLE_ANALYTICS=false

# Enable debug mode (useful for staging testing)
VITE_ENABLE_DEBUG=true

# Enable beta features (test new features in staging)
VITE_ENABLE_BETA_FEATURES=true

# Enable console logging
VITE_ENABLE_CONSOLE=true

# =============================================================================
# Deployment Metadata
# =============================================================================

# Environment tag
VITE_DEPLOYED_ENV=staging

# Build identifier (auto-generated during build)
# VITE_BUILD_ID=staging-$(date +%Y%m%d-%H%M%S)

# =============================================================================
# Optional: Performance & Monitoring
# =============================================================================

# Sentry Error Tracking
# Get your DSN from https://sentry.io/settings/projects/
# Sign up at https://sentry.io/ (free tier: 5,000 errors/month)
# Use separate project for staging or use same DSN with different environment
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
VITE_SENTRY_ENVIRONMENT=staging
VITE_SENTRY_ENABLED=true

# Optional: Set release version for tracking
# VITE_SENTRY_RELEASE=frontend-taskmanager@1.0.0-staging

# Optional: Configure sampling rates (0.0 to 1.0)
# Higher sampling in staging for thorough testing
# VITE_SENTRY_SAMPLE_RATE=1.0
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.5

# Legacy error reporting (use staging-specific endpoint)
# VITE_ERROR_REPORTING_URL=https://errors-staging.your-domain.com

# Performance monitoring
# VITE_PERFORMANCE_MONITORING=false

# =============================================================================
# Notes
# =============================================================================

# 1. Environment variables are baked into the build at compile time
# 2. After changing this file, you must rebuild: npm run build
# 3. Never commit actual API keys to version control
# 4. Use different API keys for staging vs production
# 5. Staging can have more permissive settings for testing
