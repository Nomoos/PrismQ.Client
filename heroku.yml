# Heroku deployment configuration
# This file tells Heroku how to build and deploy the application

build:
  docker:
    web: Dockerfile

# Run the web service using the Dockerfile
run:
  web: uvicorn backend.src.main:app --host 0.0.0.0 --port $PORT
